---
- hosts: ec2
  gather_facts: false
  become: yes
  pre_tasks:
  - name: Check if python2 is installed, otherwise install for Ansible
    raw: bash -c "test -e /usr/bin/python || (apt -qqy update && apt install -qqy python-minimal)"
    register: output
    changed_when: output.stdout != ""
